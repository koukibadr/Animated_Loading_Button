import 'package:flutter/material.dart';

class ConstantValues {
  static const Duration defaultDuration = Duration(
    milliseconds: 2000,
  );

  static const double defaultWidth = 200;
  static const double defaultHeight = 50;
  static const Color defaultButtonColor = Colors.blue;
  static const Color defaultIndicatorColor = Colors.blue;
  static const Color defaultIndicatorBackground = Colors.white;
  static const double defaultIndicatorSize = 5.0;
}
